name: ShellCheck

on:
  pull_request:
    paths:
      - '*.sh'
  push:
    paths:
      - '*.sh'

jobs:
  shellcheck:
    name: Shellcheck
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Run ShellCheck
        uses: ludeeus/action-shellcheck@master